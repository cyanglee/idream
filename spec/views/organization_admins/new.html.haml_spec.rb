require 'spec_helper'

describe "organization_admins/new.html.haml" do
  # before(:each) do
  #   assign(:organization_admin, stub_model(OrganizationAdmin).as_new_record)
  # end
  # 
  # it "renders new organization_admin form" do
  #   render
  # 
  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   assert_select "form", :action => organization_admins_path, :method => "post" do
  #   end
  # end
end
