%p#notice= notice

%p
  %b User:
  = @user.email

%p
  %b Role:
  -#= @user.roles.reduce([]) {|result, element| result << element.name}.join(", ")
  @filtered_roles

= link_to 'Edit', edit_user_path(@user)
\|
= link_to 'Back', users_path
